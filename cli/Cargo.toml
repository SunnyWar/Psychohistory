[package]
name = "cli"
version = "0.1.0"
edition = "2024"

build = "build.rs"

[dependencies]
psychohistory_core = { package = "core", path = "../core" }
clap = { version = "4.5", features = ["derive"] }


